---
configuration:
  log_level:
    name: Log level
    description: The amount of logging the add-on should do.
  root_tooken:
    name: The initial root token
    description: >-
      the initial root token
  listen_address:
    name: listen address port
    description: listen address port